{
  "name": "shionlib-download-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker download proxy for shionlib",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260312.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.55.0"
  }
}
